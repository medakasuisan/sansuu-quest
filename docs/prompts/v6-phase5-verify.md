# v6 Phase 5 発射プロンプト — 独立検証（Sonnet検証係・検疫つき）

> 2026-07-18 策定。発射条件: Phase 4 受領後。**検疫: Phase 4 の diff・実装係の報告・会話履歴を渡さない**。
> {BIBLE} = docs/kazan-story-bible.md（正本化後の実ファイル名に置換）。

```
あなたは「さんすうクエスト」の独立検証係です。実装の経緯は一切知らされていません。
現物と契約だけで判定してください。

## 入力
- 現物: /Users/sasaki_shin-ichi/Documents/sansu-quest/sansu-quest.html
- 契約: docs/v6-plan-kazan-area.md (特に §0 §2 §5) + {BIBLE} + docs/monster-design.md v2節

## 判定項目(各項目に PASS / FAIL / UNTESTABLE と根拠・再現手順を付ける)
1. selfTest 全パス(node+jsdom 実行・console 出力貼付)
2. かざん4ユニット(mul-basic/big/tech/word)が計画 §2-2 の生成器ロスターどおり出題される
   (各生成器を実際に引き当てて内容を確認)
3. fixture 検算:
   a. 23×4 の解説に「20×4」「80」「3×4」「12」「92」が全て含まれる
   b. 0のかけ算: answer=0 かつ「0に なにを かけても 0」を含む
   c. 23×45 の解説に「40」「920」「115」「1035」を含む
   d. 19×6 見積りの正解方向が「小さい」
   e. 文章題の解説に「じゅんばんを かえても」の趣旨の1文がある(乗法順序非依存の憲法)
4. 台本一字一句照合: 全セリフ・図鑑テキストを独立に抽出し {BIBLE} と diff → 差分ゼロ
5. 罰ゼロ: ✗時に減点・没収・煽りが存在しない(コードパス確認)
6. 回帰: 既存かずのもり(わり算3ユニット・図鑑12体・ストーリー14話・セーブ)に挙動変更がない
7. 320px レイアウト / 旧セーブ(v5.2形式)マイグレーション / マルチタブ同期
8. 単一ファイル制約: 外部リソース参照(http/https/CDN/外部フォント)が増えていない(grep 確認)
9. レア出現条件がロスター確定値(ゼロビ/ビュンビ/オヨソン)どおり実装されている

## 総合判定は正規5種のいずれか: APPROVED / APPROVED_WITH_COMMENTS / REJECTED / BLOCKED / UNTESTABLE
FAIL 項目には必ず再現手順を付けること。

## 報告形式
1. 総合判定 / 2. 項目別判定表(根拠つき) / 3. FAIL の再現手順 / 4. 実行した全コマンドと生出力
```

## 公開手順（APPROVED 後・オーケストレータ作業）
1. 佐々木さん実機(タブレット)目視 → OK
2. commit(対象明示) → Artifact republish（既存 URL: v4設計書記載の ARTIFACT_URL を維持）
3. WebFetch で実配信を確認（ブラウザキャッシュ罠のため。配信 HTML に「かけざんのかざん」文字列が
   含まれること + selfTest ログ確認）
4. 息子くんの初回アクセスは最新が届く（キャッシュ罠は既アクセス端末のみ・実害なし）
