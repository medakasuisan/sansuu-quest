# v6 Phase 4 発射プロンプト — かざんゲーム層（Sonnet実装係）

> 2026-07-18 策定。**発射条件: Phase 1 bible 正本化(DRAFT除去) + Phase 2 マージ済み + Phase 3 画像 commit 済み**。
> {BIBLE} = docs/kazan-story-bible.md（正本化後の実ファイル名に置換して発射）。

```
あなたは「さんすうクエスト」の作業担当です。会話履歴はありません。以下の情報だけで作業してください。

## プロダクト
小学3年生向け算数学習ゲーム。単一ファイルHTML(外部リソース・CDN・通信一切禁止)。
正本: /Users/sasaki_shin-ichi/Documents/sansu-quest/sansu-quest.html

## 作業前の必読ファイル(全文読むこと)
- docs/v6-plan-kazan-area.md (計画の正本。特に §5 基本原則)
- {BIBLE} (台本・図鑑テキスト・トリガー・シーン絵IDの正本)
- docs/monster-design.md「v2 ロスター かざん 12体」節
- sansu-quest.html のゲーム層(既存かずのもりの加入・給餌・進化・話解放・図鑑・相棒・セーブの実装)

## 全作業共通の鉄則
1. 【不可侵】数学ロジック層(Generators/CURRICULUM/selfTest)・既存かずのもりエリアの挙動は 1bit も変えない。
2. 【正本主義】台本・図鑑テキスト・モンスター名は {BIBLE} と monster-design.md が正本。
   一字一句変えない(要約・言い換え・句読点や改行位置の変更も禁止)。
3. 【罰ゼロ】✗の帰結は「進まないだけ」。ペナルティ演出を追加しない。
4. 【セーブ互換】sq_save_v2 に additive 追加のみ。既存キーの削除・改名・意味変更禁止。
5. 【表記】子ども向けはひらがな多め。「おうちのひと」画面のみ漢字OK。
6. git 操作はしない。ファイル編集のみ。

## タスク
「かけざんのかざん」のゲーム層を実装する。

## 実装項目
1. エリア選択: ホームに「かずのもり/かけざんのかざん」のエリア切替を実装。
   かざんの解放条件 = かずのもり図鑑12体コンプ(既存「ちずのかけら」フラグ)。
   未解放時は既存の「じゅんびちゅう」表現踏襲。エリア内のユニットカードは Phase 2 の mul-* 4枚
2. モンスター12体(カケッチ〜オヨソン): assets/monsters/webp/014〜025 を base64 化し
   既存 MONSTER_PNG と同構造で追加。図鑑を累積24体に拡張。タイプ表示「かざんタイプ」を追加。
   加入トリガー・進化しきい値(Lv5/Lv10)は {BIBLE} のトリガー欄と既存仕様に従う
3. ストーリー全14話: {BIBLE} のトリガー欄どおりに解放。1話3画面・既存会話UIを使用。
   シーン絵 K1〜K10 (assets/scenes/webp) も base64 で組み込み
4. 図鑑詳細カード: {BIBLE} の図鑑テキストを組み込み(ひみつページ/であったひ の既存仕様踏襲)
5. レア出現条件: ゼロビ=0のかけ算の問題に正解 / ビュンビ=5問連続正解 / オヨソン=見積り問題10問正解
6. セーブ: 新フィールドは additive。旧セーブ(v5.2形式)読み込み時に安全に初期化する
   マイグレーション。QR `SQ2-` コードは拡張のみ(旧コードの復元互換を維持)
7. つまずき傾向(おうちのひと画面): かざん分類(くり上がり忘れ/0のかけ算/位ずれ/見積り方向)を追加
8. おうちのひと画面に「かけ算の式の順序について」の注記1枚を追加(v6計画 §5 の文言に従う。
   数学的には順序によらず正しいこと・学校のテストでは順序を指定される場合があること)

## 検証(すべて実施・証跡=コマンド+生出力 必須)
1. 台本一字一句照合: HTML に組み込んだ全セリフ・図鑑テキストを抽出し {BIBLE} と diff して
   差分ゼロを示す(照合スクリプトの出力貼付。「目視で確認した」は不可)
2. node + jsdom: selfTest 全パス + トリガー値を満たすセーブデータを注入して該当話が
   解放されることを DOM で確認(14話ぶんの解放マトリクスを表で報告)
3. 320px 幅で新規画面(エリア切替・図鑑・会話・おうちのひと注記)のレイアウト崩れなし
4. マルチタブ同期・旧セーブマイグレーションの動作確認(v5.2形式サンプルを自作して検証)
5. git diff --stat + 既存かずのもりに挙動変更がないことの自己申告

## 報告形式(必須)
1. 変更サマリー / 2. 検証証跡(タイムスタンプ付き生出力) / 3. 未実施・懸念の正直な列挙
```
