# 契約 <番号> — <タイトル>

- 状態: **DRAFT（着手ゲート待ち）** / **LOCKED（YYYY-MM-DD 佐々木さん承認）**
- Tier: L1 / L2 / **N**（判定根拠を書く）
- 実装者: さんすう実装係（`sansuu-quest-implementer`・Sonnet）
- 正本: 参照する設計ドキュメントのパス（bible / 計画 / 設計書）

---

## 1. スコープ

### やること
（番号付きで、検証できる粒度で書く）

### やらないこと（スコープ外・触ったら契約違反）
（**不可侵領域を名指しで書く**。「既存◯◯は不可侵」だけでなく関数名・キー名まで）

---

## 2. 実装仕様

（実装係が判断に迷わない粒度で。迷う余地を残す場合は「迷ったら `### 要判断` で停止」と明記）

---

## 3. 完了条件（DoD）

| # | 条件 | 検証方法 |
|---|---|---|
| D1 | | |

**必ず入れる基本セット**
- `selfTest()` が全パス（現在の総問題数を明記する）
- Console エラー 0 件
- 320px で崩れない
- 既存機能の回帰確認（**この変更が触っていない側**を1本通してプレイする）
- `sqTrack` / `SAVE_KEY` が無変更（触る契約なら、何をどう変えるか明記）

---

## 4. セーブに触る場合の必須ブロック（`game` の構造・カウンタの意味・localStorage に触るなら必ず埋める）

> 規約 `CLAUDE.md` §2-2 / §3-2 を参照。**このブロックが空のままセーブに触る契約は LOCKED にしない。**

### 4-1. 追加・変更するキー

| キー | 追加/変更 | 意味 | 移行方式（導出 / 既定値 / 移行しない） | 理由 |
|---|---|---|---|---|

### 4-2. 壊れ方 4 種の DoD（R3）

| # | 観点 | 条件 | 検証方法 |
|---|---|---|---|
| S1 | **消えない** | 代表セーブを注入 → 所持・レベル・アメ・図鑑が保持される | 注入 JSON と起動後の `game` を両方貼る |
| S2 | **巻き戻らない** | 次のイベント（なかま加入・進化・物語）までの距離が伸びていない | 移行前後の該当カウンタを並べて貼る |
| S3 | **暴発しない** | 未読の物語・未取得のレアが一気に解放されない | 起動後の `game.story.read` / `game.rares` を貼る |
| S4 | **前に進める** | 移行後に通常プレイして、次のイベントが実際に発火する | 実機プレイのログ |

### 4-3. `selfTestSave()` へのケース追加

- 追加するケース名と、assert する内容を書く（**追加は既定。省略する場合は理由を書く**）

---

## 5. 承認記録

| 日付 | 状態 | 備考 |
|---|---|---|
