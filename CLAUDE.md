# さんすうクエスト 開発規約

> **この repo が正本**（`medakasuisan/sansuu-quest` / 本番 https://sansuu-quest.vercel.app）。
> 構成・経緯・画像追加手順は `docs/README.md`、設計正本は `docs/` を参照。
> 制定 2026-07-25。改訂は佐々木さんの承認を得てから行う。

---

## 0. このプロダクトの前提

- **利用者は小学3年生**（身内向け）。難しい語彙・否定的な言い回し・小さい文字は使わない
- **単一 HTML + 静的画像**（`index.html` + `img/*.webp`）。vanilla JS・**依存ライブラリゼロ**・**ビルド工程なし**（2026-07-27 改訂。旧: 単体 HTML 1ファイル）
- 表示画像は **`img/` 配下の WebP を同一オリジンから読み込む**（46件）。**外部ドメインからは画像を読み込まない**（広告ブロッカー耐性は同一オリジンで担保）
- 進捗は **localStorage**（サーバー送信なし）。オフライン・広告ブロッカー環境でも**ゲームは完全動作すること**
  - オフライン動作は **`sw.js`（Service Worker）が担保**する（2026-07-27〜・契約 `v7-refactor-b`）。`index.html` + 画像46枚をプリキャッシュしている

## 1. 教育設計の基本原則（変更には佐々木さんの裁定が必要）

1. **罰ゼロ設計** — 間違えても失うものを作らない。✗ は正直につけるが、責める表現にしない
2. **解説は正誤にかかわらず毎回表示する**（現行実装の仕様。指標設計でこれを忘れると `explain_views` のような無意味な指標を作る）
3. **乗法の順序非依存** — 式の順序（1つ分×いくつ分）を採点基準にしない。解説では交換法則を積極的に教える。学校テストとの差異は「おうちのひと」画面で保護者に説明する
4. 教育まわりの用語・物語は `docs/kazan-story-bible.md` / `docs/v5-story-bible.md` を正本とし、**要約や言い換えで上書きしない**

## 2. 変更の重さ（Tier）と着手ゲート

| Tier | 目安 | 着手 |
|---|---|---|
| **L1** | 文言・docstring・ドキュメント・表記ゆれ | そのまま着手してよい |
| **L2** | 局所的な実装変更（目安 +50行以内・既存構造を変えない） | **佐々木さんの承認後**に着手 |
| **N** | 新単元・ゲーム層の構造変更・セーブデータ形式の変更・計測設計の変更 | **設計を先に書いて承認** → 実装 |

- 行数に関わらず、**セーブデータ形式・localStorage キー・計測イベント名**に触る変更は **Tier N** 扱い（後方互換が壊れると子どもの進捗が消えるため）
- **実装は `sansuu-quest-implementer`（さんすう実装係・Sonnet）へ委譲する**のが既定（§8）
- 🔍 **Tier N の契約は、LOCKED する前に `sansuu-quest-contract-auditor`（契約検査係）を1回通す**（2026-07-27〜・§8）。
  L1 / L2 は通さない。検査係は**契約に書かれた事実（識別子・数値・DoD の判定条件）が実コードと整合するか**だけを見る（設計の良し悪しは判定しない）

## 2-2. セーブデータ設計の基本原則（子どもの進捗を壊さないため・2026-07-25 制定）

> 背景: Phase 4a 追補B で「森の加入判定を `totalCorrect` → `divCorrect` に変える」変更を設計したとき、
> **データは1バイトも失われないのに、カウンタの意味が変わることで「次のなかまが来るまでの進捗」だけが巻き戻る**ことが分かった。
> 「データが消えないか」しか見ない後方互換テストでは、この種の劣化はすり抜ける。

### R1. 既存キーの「意味」を変えない
`game` に入っている値の意味（何を数えているか・何の単位か）は変えない。
意味を変えたくなったら **新しいキーを足し、既存キーはそのまま残す**。

### R2. 新しいキーには「移行値の導出ルール」を必ず決める
新キーを足すときは、**既存セーブに対して何を初期値にするか**を必ず決めて契約に書く。選択肢は3つだけ:

| 方式 | 使う場面 | 例 |
|---|---|---|
| **導出**（既存の値から計算して埋める） | 過去のデータから正しい値が復元できるとき（既定） | `divCorrect` ← 旧 `totalCorrect`（かけ算が存在しなかった時代の値は全てわり算由来） |
| **既定値**（0 / false から開始） | 復元できず、巻き戻っても実害がないと**佐々木さんが判断した**とき | — |
| **移行しない**（新規セーブのみ有効） | その機能を既存ユーザーに出さないとき | — |

「なんとなく 0 から」は禁止。**どれを選んだか・なぜかを契約に書く**。

### R3. 壊れ方は「消える」だけではない
セーブ変更の検証では、次の4種類を**それぞれ**確認する。

1. **消えない**（所持・レベル・アメ・図鑑が保持される）
2. **巻き戻らない**（次のイベントまでの距離が伸びていない）
3. **暴発しない**（未読の物語や未取得のレアが一気に解放されない）
4. **前に進める**（移行後も通常プレイでイベントが発火する）

### R4. `SAVE_KEY` は変えない
`sq_save_v2` のキー名は変更しない（キー名を変えた瞬間、全ユーザーの進捗が消える）。
構造の追加は常に additive に行う。

### R5. セーブに触る変更は Tier N・fixture テスト必須
`game` の構造・カウンタの意味・`localStorage` キーに触る変更は、行数に関わらず **Tier N**。
`SAVE_FIXTURES` の代表セーブ（新規 / 進行中 / コンプ済み）を使った**機械テスト**を必ず通すこと（§3-2）。

---

## 3. 必須検証（実装したら必ず全部回す）

1. **`selfTest()` 全パス** — ブラウザコンソールで実行。問題生成器の機械保証（実測 1,200問）
2. **ローカル実機確認** — `python3 -m http.server 8000` で起動し、**実際にプレイして**変更箇所の挙動を見る
3. **Console エラーゼロ**
4. **狭幅 320px での表示崩れ確認**（実機はタブレット/スマホ）
5. 計測に触ったら **`window.dataLayer` を直接確認**してイベントとパラメータを実測する

> 「動くはず」で報告しない。**実測値（発火したイベント名・パラメータ・件数）を添えて**報告する。

## 3-2. セーブ移行の機械テスト（`selfTestSave()`）

問題生成の正しさを `selfTest()` が機械保証しているのと同じ思想で、**セーブ移行の正しさも機械保証する**。

- `SAVE_FIXTURES` … 代表セーブを固定データで持つ（最低3種: **新規** / **進行中**（ライン一部所持・カウンタ途中） / **コンプ済み**）
- `selfTestSave()` … 各 fixture を `mergeIntoDefault()` に通し、§2-2 R3 の 4 観点を assert する
- `selfTest()` の末尾から呼び、**Console 1行で総件数が出る**こと

セーブに触る契約では、この関数への**ケース追加を DoD に必ず含める**（`### 要判断` で相談せず、追加が既定）。

---

## 4. 計測（GA4）のルール

- 測定ID `G-ST5Z0S53P8`。設計正本は `.company/secretary/notes/2026-07-25-sansuu-quest-analytics-design.md`
- **送ってはいけないもの**: なまえ・入力したこたえ・問題文そのもの・セーブデータ・生の起動回数（`bootBucket()` でバケット化する）
- **`sqTrack()` を必ず経由する**（gtag 不在でも no-op。計測失敗でゲームを止めない）
- **GA4 の予約イベント名を使わない**: `page_view` / `session_start` / `first_visit` / `user_engagement`（自動収集と衝突してセッション数が汚染される）
- **起動直後のイベントは消えることを前提に設計する**（`game_launch` で実績あり）。必ず本番 DebugView で実測し、届かないなら「最初のユーザー操作時に遅延発火」へ切り替える
- 離脱検知は `pagehide` を使う。**`visibilitychange` は使わない**（モバイルのアプリ切替で誤検知し、離脱数が壊れる）
- gtag.js に SRI（`integrity`）は付けられない（Google が動的生成するため固定ハッシュが存在しない）

## 5. 画像を追加・差し替えるとき

1. 画風は `docs/monster-design.md` の**共通スタイルヘッダーを一字一句そのまま**使う（**変更禁止**）
2. 発注手順は `docs/implementation-prompts.md` §4。**Codex CLI で生成**する（`codex_run.sh` ラッパーは使わない＝無進捗検知で誤って殺されるため）
3. **watchdog は自前実装**（macOS に `timeout` / `gtimeout` は無い）
4. 生成後は**佐々木さんの目視ゲート** → OK 分だけ採用。ファビコン等の縮小用途は**実寸プレビューを添えて**判断を仰ぐ
5. **原本 PNG を `assets/` に保存してから** commit（`assets/brand/` はファビコン・OGP 用）
5-2. **表示用は WebP に変換して `img/` に置き、定数（`GUIDE_IMG` / `SCENE_IMG` / `MONSTER_PNG`）の値をそのファイル名にする**（2026-07-27〜。base64 埋め込みは廃止）。`img/` は `immutable` キャッシュのため、**差し替えは必ず新しいファイル名**で行う
6. 文字は画像に焼かない。文字が要るカードは **HTML テンプレ + playwright screenshot** で合成する（`assets/brand/ogp_card.html` が実例。`npx playwright` はブラウザ未 DL なので **python3.11 の playwright** を使う）

## 6. デプロイ

- `git push origin main` → **Vercel が自動デプロイ**（Git 連携済み）
- **push の前に必ず佐々木さんの承認を取る**（push ＝ 本番反映。ゲートを迂回・代行しない）
- `npx vercel --prod` の直叩きは禁止
- `docs/` `assets/` は `.vercelignore` で配信対象外。**`img/` は配信対象**（表示用画像の実体・2026-07-27〜）
- `vercel.json` で `img/*` に `max-age=31536000, immutable` を付けている。**画像を差し替えるときは必ず新しいファイル名にする**（同名で上書きすると古い絵がキャッシュから消えない）
- push 後は **60秒待って `npx vercel ls` 単発** → 本番 smoke（`curl` はブラウザ UA 必須）。ポーリングで粘らない
- 🔴 **`sw.js` を変更したら、`CACHE_NAME` のバージョンを必ず上げる**（上げないと `activate` の旧キャッシュ破棄が走らない）
- 🔴 **毎デプロイの smoke に「更新が届くこと」の確認を含める**: 本番を開き直して**リロード1回で新しい内容が出る**こと。SW の事故は「本番では古い版のまま」という形で出るので、デプロイ直後に気づけないと長期化する

## 7. git 規律・ドキュメント同期

- `git add -A` は使わない。**対象ファイルを明示指定**して add する
- **`--no-verify` は禁止**。hook が落ちたら原因を分析して直す
- **changelog・日報・decisions などの履歴は訂正しない**（当時の記録として残す）
- **README の「送信しません」表記は実装と必ず同期させる**（計測を足したら README も同じ commit で直す）
- 制度・方針を変えたら `docs/README.md` と本規約の該当行も**同じターンで**書き換える（「書くのをやめる」だけの廃止は禁止）

## 8. 委譲（誰がやるか）

| 仕事 | 担当 |
|---|---|
| 実装（L1/L2、および承認済み N の実装工程） | **`sansuu-quest-implementer`（さんすう実装係・Sonnet）** |
| **Tier N 契約の LOCKED 前検査** | **`sansuu-quest-contract-auditor`（契約検査係・Sonnet・read-only）** |
| 画像生成 | Codex CLI（§5） |
| 設計・裁定・Tier 判定・承認取得・commit / push | 秘書 + 佐々木さん（**委譲しない**） |

### 契約検査係を置いた理由（2026-07-27）

`v7-refactor-a` / `v7-refactor-b` の 2 本で、**秘書が起草した契約に 5 件の誤り**があり、**5 件とも実装係が実装中に発見して差し戻した**。
5 件すべて「起草時に実際にコマンドを打つか、数字を検算すれば分かること」だった:

| # | 誤り | 類型 |
|---|---|---|
| 1 | `MON_IMG` が実在しない定数だった（実在は `MONSTER_PNG`） | 識別子の実在未確認 |
| 2 | DoD「`data:image` 0件」が達成不可能（SVG もマッチする） | DoD 判定コマンドの未実行 |
| 3 | DoD「200KB以下」が chars / bytes の取り違え | 数値の単位取り違え |
| 4 | プリキャッシュ「48件」が足し算ミス（正しくは47） | 内訳と合計の不整合 |
| 5 | DoD「両状態で Console エラー0件」が達成不可能 | DoD 判定条件の実行可能性未検証 |
| 6 | 「原本 PNG **95** ファイル」が実は全ファイル数（PNG は 48） | **数値のラベルと実体の不一致** |

※ 1〜5 は実装係が実装中に発見。**6 は新設した検査係が初回の試運転で自力で検出した**（実装 commit は `assets/` に diff 0 件のため実害なし）。
6 は「数値そのものは正しいが、何を数えた数かというラベルがズレている」型で、**合計の検算だけでは見つからない**。定義の `C4-(c)` に実例として反映済み。

**契約を書くのも検証するのも秘書**という自己評価バイアスが原因。検査係はこの 6 類型（定義ファイルの `C1`〜`C5`）を機械照合するためだけに存在する。
ジーニー部の `genie-contract-auditor`（リスクの網羅検出）とは目的が違う**軽量版**であり、**範囲を絞ることが軽さの本体**。設計の良し悪しやリスクの網羅には踏み込ませない。

- 実装係には**契約（やること・完了条件）と対象ファイルだけ**を渡す。誘導文・自己評価・会話履歴は渡さない
- 実装係の成果物主張（変更したファイル・実測値）は、秘書が**1コマンドで実在確認**してから次工程へ進む

## 9. 現在の状態（2026-07-27 時点のスナップショット・現況は git log を正とする）

- 有効な単元: **7種**（わり算 3種 `div-basic` / `div-remainder` / `div-word` ＋ **かけ算 4種** `mul-basic` / `mul-big` / `mul-tech` / `mul-word`）。`enabled:false` は **`hissan` のみ**
  - ※ 2026-07-25 時点の本節は「かけ算は `enabled:false`」と記載していたが、同日 21:04 の v6 Phase 4b（`f1d377c`）で かざんエリアが開通し実態が変わった。現況を反映して更新（2026-07-27 実測）
- 旧 repo `~/Documents/sansu-quest` は**マージ元にしない**（参考資料・2026-07-25 凍結）
- v6 かざんエリアは **Phase 2 → 3 → 4a → 4b → 5 まで全完走**（Phase 5 統合検証は 2026-07-27 に実機目視完了）
- GA4 Phase 1 の6イベント実装済み・favicon / OGP 設定済み
- 表示画像は **46枚**（`GUIDE_IMG` 1 / `SCENE_IMG` 21（`s01`–`s11` + `K1`–`K10`）/ **`MONSTER_PNG`** 24（`1`–`25`・13欠番））
- **リファクタリング（案B）は Phase 5 まで完了**（2026-07-28・設計正本 `docs/refactor-plan-2026-07-27.md`）
  - Phase 2+3: 画像46枚を `img/` へ外部化（契約 `v7-refactor-a`・`index.html` 1,906,768 → 251,891 bytes）
  - Phase 4: `sw.js` によるオフライン担保 + `GUIDE_IMG` の preload（契約 `v7-refactor-b`）
  - Phase 5: QR_ENCODE を「ここから/ここまで」バナーで視覚的に隔離（分離はしない）+ `selfTest()` / `SAVE_FIXTURES` / `selfTestSave()` を **boot ブロック直前の「開発用」ブロック**へ集約（2026-07-28・L2）
    - 🔴 **末尾ではなく boot 直前に置いた理由**: `SAVE_FIXTURES` は `const` で、`selfTest();` の呼び出しより後ろに定義を移すと **TDZ の `ReferenceError` で起動が停止する**。`function` 宣言はホイストされるため「関数は動くが `const` だけ落ちる」形で出る。この配置は変更しないこと
    - 移動は純粋な cut & paste（中身は1バイト未変更）。行の多重集合 diff で「追加4行 / 削除1行」のみを機械確認済み
  - **Phase 2+3 / Phase 4 は本番反映済み**（`origin/main` = `0fc9d9c`）。2026-07-28 に本番実測で確認: `sw.js` = 200 / `index.html` の `data:image/webp` 残存 = 0 件
    - ※ 本節は 2026-07-27 時点で「本番未反映（`origin/main` は `f1d377c`）」と記載していたが、同日 20:23 の `0fc9d9c` push で実態が変わっていた。現況を反映して更新（2026-07-28 実測）
  - **未 push**: `58c86b7` / `9c8932e`（契約検査係の定義・規約）。本番挙動に影響しないため次回 push に同乗させる
